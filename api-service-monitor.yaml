apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: api-service-monitor
spec:
  selector:
    matchLabels:
      app: api-service
  namespaceSelector:
    matchNames:
      - banking-app
  endpoints:
    - port: http
      path: /metrics
      interval: 30s