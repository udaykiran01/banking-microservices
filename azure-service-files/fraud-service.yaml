apiVersion: v1
kind: Service
metadata:
  name: fraud-service
  namespace: banking-dev
spec:
  selector:
    app: fraud-service
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP