# entra-mfa-conditional-access

Reusable module for enforcing MFA through Microsoft Entra Conditional Access for the AWS IAM Identity Center enterprise application.

The policy targets the enterprise application by service principal display name and grants access only when MFA is satisfied.
