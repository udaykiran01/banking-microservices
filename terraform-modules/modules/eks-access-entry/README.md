# eks-access-entry

Creates an Amazon EKS access entry for a federated IAM principal.

Use this for human access from IAM Identity Center roles into EKS. Pair it with Kubernetes RBAC that binds the configured Kubernetes group to namespaced pod permissions.
