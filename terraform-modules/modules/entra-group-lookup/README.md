# entra-group-lookup

Looks up an Entra group that has been synced into AWS IAM Identity Center Identity Store.

The lookup uses the Identity Store `DisplayName`, which should match the synced Entra group display name.
