apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: banking-app
  labels:
    app: frontend-service
spec:
  selector:
    app: frontend-service
  ports:
    - name: http
      port: 8080
      targetPort: http
