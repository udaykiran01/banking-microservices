apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: banking-api
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - banking-dev
  selector:
    matchLabels:
      app: banking-api
  endpoints:
    - port: http
      path: /metrics
      interval: 15s